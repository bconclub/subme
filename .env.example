# Supabase - leave unset for local-only mode (all data on device).
# When the Subme Supabase project is provisioned:
#  1. apply supabase/migrations/*.sql in order
#  2. fill these in and rebuild
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
